# Local data and secrets — the board lives in ~/.kiarez-space, not in the repo.
.env
*.db
supabase-export.json

# Python
__pycache__/
*.py[cod]
.venv/

# Legacy local observation log (the canonical one is ~/.claude/skill-observations)
skill-observations/
